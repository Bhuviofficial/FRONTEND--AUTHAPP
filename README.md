# Thios Project

This project is a simple authentication system built with React and Node.js.

## Installation

To install the project, you will need to clone the repository and install the dependencies for both the frontend and the backend.

### Frontend

Run the following commands in the frontend directory:

### Frontend

1. Clone the frontend repository with the following command:
   
git<clone>  https://github.com/Bhuviofficial/FRONTEND--AUTHAPP